import type React from "react"
import type { Metadata } from "next"
import { GeistSans } from "geist/font/sans"
import { GeistMono } from "geist/font/mono"
import { Analytics } from "@vercel/analytics/next"
import { Suspense } from "react"
import "./globals.css"

export const metadata: Metadata = {
  title: "Stratosphere - Code with Your Voice",
  description:
    "Professional development tool that uses speech instead of text. Build and edit code repositories on the go with voice commands.",
  generator: "v0.app",
  icons: {
    icon: "/stratosphere-icon.svg",
    shortcut: "/stratosphere-icon.svg",
    apple: "/stratosphere-icon.svg",
  },
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode
}>) {
  return (
    <html lang="en">
      <body className={`font-sans ${GeistSans.variable} ${GeistMono.variable} antialiased`}>
        <Suspense fallback={null}>{children}</Suspense>
        <Analytics />
      </body>
    </html>
  )
}
